import 'package:flutter/material.dart';

class BookAppointmentData {
  String? id;
  String? title;
  Widget? widget;
  double? progress;

  BookAppointmentData({this.id, this.title, this.widget, this.progress});
}
